#include <stdio.h>
#include <stdlib.h>
#include "match.h"
#include "rule.h"
#include "vector.h"


struct match
check_match(struct vector *vector, struct rule *rule){
	//missing stuff
	struct match retTest;
	return retTest;
}

void
print_match(struct match *match){
	puts("TEST"); //Test
}
